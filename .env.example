# 复制为 .env.development / .env.production 并按环境填写（可选）
# 未配置时由 src/lib/api-base.ts 回退：dev → localhost:8787，生产构建 → https://api.craft.luminolsuki.moe
# 注意：严禁在此放置 BETTER_AUTH_SECRET / QQ_CLIENT_SECRET 等后端机密
VITE_API_BASE_URL=https://api.craft.luminolsuki.moe
